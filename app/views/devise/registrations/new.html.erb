<div class="panel panel-default">

	<div class="panel-heading">
	<h2>Sign up</h1>
	</div> <!-- end panel heading.  -->

	<div class="panel-body">
		<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
		  <%= devise_error_messages! %>

          <div class="form-group">
      	    <%= f.label :name %>
      	    <%= f.text_field :name, autofocus: true, class: "form-control" %> 
          </div>

		  <div class="form-group">
			  <%= f.label :email %>
			  <%= f.email_field :email, autofocus: true, class: "form-control" %> <!-- how you add a class="form-control to a embedded ruby object.  -->
			  <!-- :autofocus => true is old ruby syntax. autofocus: true is new ruby syntax.  -->
		  </div>

		 <div class="form-group">
			  <%= f.label :password %>
			  <%= f.password_field :password, class: "form-control" %>
		  </div>

		  <div class="form-group">
		  	<%= f.submit "Sign up", class: "btn btn-primary" %>
		  </div>
		<% end %>

		<div class="panel-footer">
			<%= render "devise/shared/links" %>
		</div> <!-- end panel footer -->
		
	</div> <!-- end panel body -->
</div> <!-- end panel -->


new.html.erb views>devise>registrations

